{{- if .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "heureum.fullname" . }}
  labels:
    {{- include "heureum.labels" . | nindent 4 }}
spec:
  rules:
  - host: {{ .Values.ingress.host }}
    http:
      paths:
      - path: /api
        pathType: Prefix
        backend:
          service:
            name: {{ include "heureum.fullname" . }}-api
            port:
              number: 8080
      - path: /ws
        pathType: Prefix
        backend:
          service:
            name: {{ include "heureum.fullname" . }}-api
            port:
              number: 8080
      - path: /
        pathType: Prefix
        backend:
          service:
            name: {{ include "heureum.fullname" . }}-frontend
            port:
              number: 80
{{- end }}
